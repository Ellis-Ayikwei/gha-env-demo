name: Test Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: echo "🚀 Deploying to TEST environment"

      # Build or generate artifact
      - run: mkdir -p dist && echo "Test build for $GITHUB_SHA" > dist/build.txt

      # Upload artifact for prod workflow
      - uses: actions/upload-artifact@v4
        with:
          name: test-artifact
          path: dist/
